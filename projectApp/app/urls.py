from django.urls import path
from . import views
from django.contrib import admin

urlpatterns = [
    # path('app/', views.home_view),
    # path('admin/', admin.site.urls),
    # path('about/', views.about_view),
]
